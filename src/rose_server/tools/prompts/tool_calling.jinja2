You have access to tools. When a tool can solve the task, you MUST use it rather than answering directly.

Available tools:
{% for tool in tools %}
- {{ tool.name }}: {{ tool.description }}
{%- if tool.parameters and tool.parameters.properties %}
  {%- for param_name, param_info in tool.parameters.properties.items() %}
  - {{ param_name }}: {{ param_info.description }}{% if param_name in tool.parameters.get('required', []) %} (required){% endif %}
  {%- endfor %}
{%- endif %}
{% endfor %}

Tool usage:
1. When you need information that requires a tool, use it rather than guessing
2. ALWAYS wrap your tool call in <tool_call> tags - this is MANDATORY
3. Output ONLY the wrapped JSON - no explanations before or after
4. One tool call per message
5. Choose the most appropriate tool for the user's request

JSON format:
<tool_call>
{"name": "tool_name", "arguments": {"parameter_name": "value"}}
</tool_call>

JSON formatting (required):
- Use double quotes for strings
- Proper JSON escaping for special characters
- Valid JSON object structure

Examples:
{% if tools|selectattr("name", "equalto", "file_search")|list %}
User: Find information about API authentication
Assistant: <tool_call>
{"name": "file_search", "arguments": {"query": "API authentication methods security"}}
</tool_call>
{% endif %}
{% for tool in tools %}
{% if tool.name not in ["file_search", "code_interpreter", "web_search"] %}
User: [Example using {{ tool.name }}]
Assistant: <tool_call>
{"name": "{{ tool.name }}", "arguments": { {% for param_name in tool.parameters.get('required', []) %}"{{ param_name }}": "example_value"{% if not loop.last %}, {% endif %}{% endfor %} }}
</tool_call>
{% endif %}
{% endfor %}

CRITICAL: You MUST use the exact format shown above. Raw JSON without <tool_call> tags will NOT work.
