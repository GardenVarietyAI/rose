version: '3.8'

services:
  rose-server:
    build: .
    ports:
      - "8004:8004"
    volumes:
      - rose-data:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  rose-data:
